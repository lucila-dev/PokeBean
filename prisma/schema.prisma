generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id           String   @id @default(cuid())
  email        String   @unique
  passwordHash String
  name         String?
  image        String?  // profile picture URL e.g. /uploads/avatars/xxx.jpg
  createdAt    DateTime @default(now())
  cards        Card[]
}

model Card {
  id          String   @id @default(cuid())
  userId      String
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  name        String
  displayName String?  // TCGPlayer-style: "Card Name - Set Name (X/YY)"
  description String?
  year        Int?
  setName     String?
  rarity      String?
  cardNumber     String?
  imageUrl       String?
  marketPrice    Float?   // User-entered (no API); optional
  priceUpdatedAt DateTime? // When marketPrice was last set/updated
  createdAt      DateTime @default(now())

  @@index([userId])
}
